{% extends 'base.html.twig' %}

{% block title %}Univers{% endblock %}

{% block body %}
    <h1>Univers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ univer.id }}</td>
            </tr>
            <tr>
                <th>Nb_galaxie</th>
                <td>{{ univer.nbGalaxie }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ univer.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('univers_index') }}"><button type="button" class="btn btn-success">back to list</button></a>

    <a href="{{ path('univers_edit', {'id': univer.id}) }}"><button type="button" class="btn btn-primary">edit</button></a>

    {{ include('univers/_delete_form.html.twig') }}
{% endblock %}
