{% extends 'base.html.twig' %}

{% block title %}Planete{% endblock %}

{% block body %}
    <h1>Planete</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planete.id }}</td>
            </tr>
            <tr>
                <th>Nb_lune</th>
                <td>{{ planete.nbLune }}</td>
            </tr>
            <tr>
                <th>Nb_satelite</th>
                <td>{{ planete.nbSatelite }}</td>
            </tr>
            <tr>
                <th>Distance_etoile</th>
                <td>{{ planete.distanceEtoile }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ planete.nom }}</td>
            </tr>
            <tr>
                <th>Systeme Solaire</th>
                <td>{{ planete.ids.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('planete_index') }}"><button type="button" class="btn btn-success">back to list</button></a>

    <a href="{{ path('planete_edit', {'id': planete.id}) }}"><button type="button" class="btn btn-primary">edit</button></a>

    {{ include('planete/_delete_form.html.twig') }}
{% endblock %}
